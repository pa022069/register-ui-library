export const REGEX_FORMAT = /^[0-9]{4}-[0-9]{4}-[0-9]{4}-[0-9]{4}$/;
export const REGEX_NUMBER = /^\d+$/;

export const STATUS = Object.freeze({
  EMPTY: '0',
  VALID: '1',
  ERROR: '2',
  ALERT: '3',
});
